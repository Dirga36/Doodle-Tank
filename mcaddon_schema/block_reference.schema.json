block_reference:1.10.0:{
    string "name"
    object "states"
    {
        bool "\w*:?\w+" : opt
        int "\w*:?\w+" : opt
        string "\w*:?\w+" : opt
    }
}

chance_information:1.14.0:{
    int "numerator"<0-*>
    int "denominator"<1-*>
}